Import('env')

env.Append(
    CCFLAGS = ['-static'],
    LINKFLAGS = ['-static'],
)

bootloader = env.Program(
    target = 'bootloader',
    source = ['main.c'],
    LIBS = ['tar'],
)

Return('bootloader')
